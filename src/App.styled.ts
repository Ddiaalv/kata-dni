import styled from 'styled-components';

const Navigation = styled.nav`
  background: #fff;
  padding: 20px;
  display: flex;
  justify-content: center;
  font-size: 20px;
  border-radius: 40px;
  a {
    margin: 10px;
    color: rgba(242, 97, 123, 1);
    text-decoration: none;
  }
  a:hover {
    color: rgba(185, 60, 168, 1);
  }
`;

export { Navigation };
